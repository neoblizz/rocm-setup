name: example

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: rocm-setup
      uses: neoblizz/rocm-setup@v0.1.0-beta
      with:
        rocm: '5.5.0'
